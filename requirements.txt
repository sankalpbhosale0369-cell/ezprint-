# EzPrint root requirements (dev convenience only).
#
# The real dependency manifests live with the services that use them:
#   - ezprint-backend/pyproject.toml (+ lock)   FastAPI / Uvicorn / SQLAlchemy / MinIO / Redis
#   - shopkeeper_app/requirements-client.txt    Windows agent (PyQt5 + requests + websocket-client)
#
# This file is kept only so `pip install -r requirements.txt` on a dev box
# pulls the shopkeeper agent's runtime deps. The legacy Flask / SocketIO /
# eventlet / Cloudinary / gunicorn packages have been removed; all of that
# lives in ezprint-backend now.

-r shopkeeper_app/requirements-client.txt
